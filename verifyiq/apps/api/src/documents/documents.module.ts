// apps/api/src/documents/documents.module.ts
import { Module } from '@nestjs/common';

// Document modules are handled via the ML service
// NestJS routes document-specific requests to the ML client
// This module is a placeholder for document-type-specific NestJS validators

@Module({})
export class DocumentsModule {}
